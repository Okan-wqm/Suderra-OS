#!/usr/sbin/nft -f
#
# Suderra OS — provisioning firewall
#
# İlk kurulumda IP üzerinden SSH ile bağlanmak için yalnızca port 22 inbound
# açıktır. suderra-lockdown çalışınca appliance firewall'a geçilir.
#

flush ruleset

table inet filter {
    chain input {
        type filter hook input priority filter; policy drop;

        iif "lo" accept
        ct state established,related accept

        # Provisioning SSH. Edge kurulumu tamamlanınca dropbear maskelenir ve
        # firewall appliance moduna döner.
        tcp dport 22 ct state new limit rate 10/minute accept

        ip protocol icmp icmp type { echo-request, destination-unreachable, time-exceeded, parameter-problem } limit rate 10/second accept
        ip6 nexthdr icmpv6 icmpv6 type { destination-unreachable, packet-too-big, time-exceeded, parameter-problem, echo-request, nd-neighbor-solicit, nd-router-advert, nd-neighbor-advert } limit rate 10/second accept
    }

    chain forward {
        type filter hook forward priority filter; policy drop;
    }

    chain output {
        type filter hook output priority filter; policy drop;

        oif "lo" accept
        ct state established,related accept

        udp dport 53 accept
        tcp dport 53 accept
        udp dport 123 accept
        tcp dport 443 accept
        tcp dport 8883 accept
        tcp dport 502 accept
        tcp dport 4840 accept
    }
}
