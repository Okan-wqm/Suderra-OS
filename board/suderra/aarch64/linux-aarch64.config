#
# Suderra OS — aarch64 kernel config (Faz 1 placeholder)
#
# Mimari-spesifik ayarlar:
#   - ARM64 platform driver'ları
#   - BCM2711/2837 (Pi CM4) veya x86 embedded
#   - GPIO/I2C/SPI subsystem'leri (Edge Agent gerekli)
#   - PWM (eğer kontrol için)
#   - Hardware watchdog (BCM2835)
#   - Device tree desteği
#   - U-Boot integration
#
# Sertleştirme: kernel-fragment.config ile birleştirilir
