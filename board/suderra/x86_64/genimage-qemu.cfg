# Suderra OS — QEMU x86_64 disk image layout
#
# Production'dan (A/B + /data) farklı olarak QEMU için TEK rootfs.
# Faz 1 boot-test bu layout'u kullanır.
#
# Çağrı: post-image.sh içinden (BR2_ROOTFS_POST_SCRIPT_ARGS=suderra_qemu_x86_64)

image disk.img {
    hdimage {
        partition-table-type = "gpt"
        gpt-location = 32K
    }

    partition efi {
        partition-type-uuid = "C12A7328-F81F-11D2-BA4B-00A0C93EC93B"
        partition-label = "EFI"
        bootable = "true"
        image = "efi-part.vfat"
        size = 32M
    }

    partition rootfs {
        partition-type-uuid = "0FC63DAF-8483-4772-8E79-3D69D8477DE4"
        partition-label = "rootfs"
        image = "rootfs.ext4"
        size = 256M
    }
}

# EFI partition içeriği — GRUB2 BIOS modu için
image efi-part.vfat {
    vfat {
        files = {
            "EFI/BOOT/BOOTX64.EFI",
            "EFI/BOOT/grub.cfg",
            "bzImage",
        }
    }
    size = 32M
}
